singularity {
	enabled = true
}

docker {
	enabled = false
}

process {

   executor = 'slurm'

	 withName:bamToFastq {
	    clusterOptions = '--reservation=interactive'
			cpus = '8'
			memory = { 4.GB * task.attempt }
	 }

	 withName:trinity {
			clusterOptions = '--qos=short'
			cpus = '6'
			memory = { 20.GB * task.attempt }
	 }
}
